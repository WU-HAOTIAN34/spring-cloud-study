server:
  port: 80

spring:
  application:
    name: cloud-order-consumer

  cloud:
    consul:
      port: 8500
      host: localhost
      discovery:
        prefer-ip-address: true
        service-name: ${spring.application.name}

